-- Добавляет ГЛАВНЫЙ_ТЕХНИЧЕСКИЙ_ДИРЕКТОР в SystemRole — та же роль по правам,
-- что и АДМИН (см. schema.prisma и lib/auth/roles.ts::isFullAdmin), но
-- отдельное значение, чтобы UI мог показывать её отдельным блоком на
-- странице "Руководители" и отдельной подписью в профиле/сайдбаре.
ALTER TYPE "SystemRole" ADD VALUE 'ГЛАВНЫЙ_ТЕХНИЧЕСКИЙ_ДИРЕКТОР';
